using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScriptTest : MonoBehaviour {


	// Use this for initialization
	void Start () {

		//VARIABLES

//		int count = transform.childCount;
//		Vector3 count = transform.eulerAngles;
//		bool count = transform.hasChanged;
//		int count = transform.hierarchyCapacity;
//		int count = transform.hierarchyCount;
//		Vector3 count = transform.localPosition;
//		Transform count = transform.parent;
	
//		Vector3 count = transform.position;
//		Debug.Log (count);
//		count.x = 0;
//		count.y = 0;
//		count.z = 0;
//		Debug.Log (count);

//		Vector3 count = transform.right;
//		Transform count = transform.root;
//		Quaternion count = transform.rotation;
//		Vector3 count = transform.up;


		//PUBLIC FUNCTIONS

//		transform.DetachChildren ();

//		GameObject count =  GameObject.Find("1");
//		Debug.Log (count);
//		Destroy (count, 2);

//		Transform count = transform.GetChild (1);
//		Debug.Log (count);

//		bool count = transform.IsChildOf(transform.GetChild(1));
//		Debug.Log (count);

	}

	// Update is called once per frame
	void Update () {

//		transform.LookAt (traget);

	}

}


